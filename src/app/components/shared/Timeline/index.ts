import {
  TimelineButton,
  TimelineContent,
  TimelineButtonContent
} from "./TimelineStyle"

export { default as Timeline } from "./Timeline"

export const TimelineStyles = {
  TimelineButton,
  TimelineContent,
  TimelineButtonContent
}
