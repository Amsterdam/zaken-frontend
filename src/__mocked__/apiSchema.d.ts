declare namespace MockComponents {
  namespace Schemas {
    export type Team = {
      readonly id: number
      readonly title: string
      readonly enabled: boolean
    }
    export type Reason = {
      readonly id: number
      readonly title: string
      readonly enabled: boolean
    }
  }
}